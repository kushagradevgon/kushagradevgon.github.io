'use client';

/**
 * No-op: custom cursor is now implemented in CSS only (see globals.css)
 * to avoid lag from JS/Framer Motion. This component is kept for API compatibility.
 */
export function CustomCursor() {
  return null;
}
